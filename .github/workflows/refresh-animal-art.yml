name: Пересобрать картинки животных

on:
  push:
    paths:
      - ".github/workflows/refresh-animal-art.yml"

permissions:
  contents: write

jobs:
  rebuild:
    runs-on: ubuntu-latest
    steps:
      - name: Получить репозиторий
        uses: actions/checkout@v5
        with:
          ref: main

      - name: Установить Pillow
        run: python -m pip install --quiet pillow

      - name: Собрать новый stock-style спрайт животных
        run: python scripts/build_animal_sprite_v4.py

      - name: Переключить тренажёр на новый спрайт
        run: |
          python - <<'PY'
          from pathlib import Path
          import re

          p = Path('assets/animals-topic.js')
          s = p.read_text(encoding='utf-8')
          s = s.replace('assets/generated/study-animals-v3.webp', 'assets/generated/study-animals-v4.webp')
          p.write_text(s, encoding='utf-8')

          p = Path('assets/animals-card-art-fix.js')
          s = p.read_text(encoding='utf-8')
          s = re.sub(r"const SRC='[^']+';", "const SRC='./assets/generated/study-animals-v4.webp?v=1';", s, count=1)
          s = re.sub(r"const FALLBACK='[^']+';", "const FALLBACK='./assets/generated/study-animals-v4.webp?v=1';", s, count=1)
          p.write_text(s, encoding='utf-8')

          p = Path('assets/trainer-v13-fixes.js')
          s = p.read_text(encoding='utf-8')
          s = s.replace('assets/animals-card-art-fix.js?v=3', 'assets/animals-card-art-fix.js?v=4')
          s = s.replace('assets/animals-topic.js?v=6', 'assets/animals-topic.js?v=7')
          p.write_text(s, encoding='utf-8')

          p = Path('index.html')
          s = p.read_text(encoding='utf-8')
          s = s.replace('assets/trainer-v13-fixes.js?v=20260917animals2', 'assets/trainer-v13-fixes.js?v=20260917animals3')
          p.write_text(s, encoding='utf-8')
          PY

      - name: Проверить новый WebP
        run: |
          python - <<'PY'
          from PIL import Image
          p='assets/generated/study-animals-v4.webp'
          im=Image.open(p)
          im.load()
          assert im.size == (1350,600), im.size
          assert im.mode in ('RGBA','RGB'), im.mode
          assert im.getbbox() is not None
          print('OK', im.format, im.mode, im.size)
          PY

      - name: Сохранить новый спрайт и подключения
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/generated/study-animals-v4.webp assets/animals-topic.js assets/animals-card-art-fix.js assets/trainer-v13-fixes.js index.html
          if git diff --cached --quiet; then
            echo "Изменений нет"
            exit 0
          fi
          git commit -m "Redraw all animal artwork in stock illustration style"
          git push origin HEAD:main
